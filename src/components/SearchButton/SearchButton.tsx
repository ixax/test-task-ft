import './SearchButton.scss';

import * as b_ from 'b_';
import React from 'react';

const b = b_.with('search-button');

export default function SearchButton() {
    return (
        <div className={b()} />
    );
}
